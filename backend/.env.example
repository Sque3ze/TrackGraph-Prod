# Backend configuration (.env example)

# Set to 0 to allow outbound Spotify API calls for image/metadata hydration
# Leave unset or 1 for demo: cache-only, no network calls are made.
DEMO_MODE=0

# In demo mode, request logging, Prometheus /metrics, and tracing are disabled.
# Set DEMO_MODE=0 in production to enable logging/metrics. Tracing will only start
# if OTEL_EXPORTER_OTLP_ENDPOINT is provided by the runtime (e.g., App Runner/ADOT).

# Spotify credentials (required when DEMO_MODE=0)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
