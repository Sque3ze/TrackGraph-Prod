# Default environment used by docker-compose for demo/testers
# Override by creating backend/.env with DEMO_MODE=0 and credentials.

DEMO_MODE=1

# Leave credentials empty in demo
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
# In demo mode, logging, /metrics, and tracing are disabled automatically.
